<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Enable component scanning -->
    <context:component-scan base-package="net.whydah.admin"/>

    <!-- Property placeholder configurer -->
    <context:property-placeholder
            location="classpath:useradminservice.properties, classpath:useradminservice_override.properties"
            ignore-resource-not-found="true"/>

    <!-- Define Jackson ObjectMapper bean -->
    <bean id="objectMapper" class="com.fasterxml.jackson.databind.ObjectMapper"/>

    <!-- Explicitly define SecurityFilter to ensure proper instantiation -->
    <bean id="securityFilter" class="net.whydah.admin.security.SecurityFilter"/>

</beans>