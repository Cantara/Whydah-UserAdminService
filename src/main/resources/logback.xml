<configuration scan="true" scanPeriod="60 seconds">

    <property name="LOG_DIR" value="logs/"/>
    <property name="appName" value="UserAdminService"/>

    <!--
        LevelChangePropagator keeps JUL levels in sync with Logback.
        This means JUL records are filtered at the JUL level before
        reaching the SLF4J bridge - avoids double evaluation overhead.
        resetJUL=true removes any existing JUL handlers so nothing
        bypasses the bridge.
    -->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>

    <!-- ================================================================ -->
    <!-- Appenders                                                         -->
    <!-- ================================================================ -->

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}${appName}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}${appName}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>5</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
            <maxFileSize>50MB</maxFileSize>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{35} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Async wrapper so request threads never block on file I/O -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
    </appender>

    <!-- ================================================================ -->
    <!-- Logger levels                                                     -->
    <!-- ================================================================ -->

    <logger name="org.eclipse.jetty"                           level="INFO"/>
    <logger name="org.glassfish.jersey"                        level="WARN"/>
    <logger name="com.hazelcast"                               level="INFO"/>
    <logger name="org.springframework"                         level="INFO"/>
    <logger name="net.whydah.sso.application.helpers"          level="INFO"/>
    <logger name="net.whydah"                                  level="INFO"/>
    <logger name="net.whydah.admin"                            level="DEBUG"/>
    <logger name="sun.net.www.protocol.http.HttpURLConnection"  level="INFO"/>

    <!-- ================================================================ -->
    <!-- Root                                                              -->
    <!-- ================================================================ -->

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>

</configuration>